<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>OAuth Tutorial</title>
        <style>
            body {
                font-family: Arial;
                margin: 0
            }

            nav {
                background: #ff5353;
                padding: 20px 10px;
            }

            nav ul {
                max-width: 960px;
                margin: 0 auto;
                padding: o;
            }

            nav li {
                list-style-type: none;
                display: inline-block;
                margin: 0 10px 0 0;
            }

            nav a {
                color: #fff;
                font-size: 18px;
                background: rgba(255, 255, 255, 0.2);
                text-decoration: none;
                padding: 10px;
                display: block;
            }

            main, header {
                max-width: 960px;
                margin: 30px auto;
                padding: 0 10px;
                color: #333;
            }

            .thumbnail {
                max-width: 45px;
                max-height: 45px;
                border: solid 3px rgba(255, 127, 0, 0.5);
                box-shadow: 0 0 0 1pt black;
                border-radius: 4px;
                display: inline-block;
                position: relative;
                top: 15px;
            }

            .thumbnail img {
                width: 100%;
                height: 100%;
            }

             .delete-btn {
                color: #fff;
                text-decoration: none;
                font-size: 18px;
                padding: 10px;
                background: #f00;
            }

            .pfp {
                margin: 20px;
                padding-left: 30px;
            }
        </style>
    </head>
    <body>
        <%- include("navbar", { user: user }) %>
        <header>
            <h1>Welcome to your profile page, <% if(user.thumbnail) { %><div class="thumbnail"><img src="<%= user.thumbnail %>" alt="thumbnail" /></div><% } %> <%= user.username %></h1>
        </header>
        <main>
            <% if(user.thumbnail) { %>
            <p>Your profile picture: </p>
            <div class="pfp">
                <img src="<%= user.thumbnail %>" />
            </div>
            <% } else { %>
            <p>No profile picture</p>
            <% } %>
            <div>
                <a class="delete-btn" href="/profile/delete">Delete user</a>
            </div>
        </main>
    </body>
</html>